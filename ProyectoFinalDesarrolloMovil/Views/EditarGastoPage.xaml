<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoFinalDesarrolloMovil.Views.EditarGastoPage"
             Title="EditarGastoPage"
             BackgroundColor="{DynamicResource Fondo}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">
            <Label Text="Editar Gasto" FontSize="24" TextColor="{DynamicResource Texto}" />

            <Entry Placeholder="Descripción" Text="{Binding Descripcion}" />
            <Entry Placeholder="Monto" Keyboard="Numeric" Text="{Binding Monto}" />
            <DatePicker Date="{Binding Fecha}" />

            <Button Text="Guardar Cambios" Command="{Binding GuardarCambiosCommand}" />
        </VerticalStackLayout>
    </ScrollView>
    
</ContentPage>